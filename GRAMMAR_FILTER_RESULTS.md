# 独立语法过滤器：本轮结果

实现：`bert_simple/grammar_filter.py`。训练：`examples/train_independent_grammar.py`。
采用独立 embedding、分句范围内的相对位置 attention、相邻 token 输入、8 维
语法表示和 8 维属性表示，以及独立属性 Q/K。主 BERT 不参与该训练。

本轮 100 epochs、随机种子 17，6096 条合成训练样本、1524 条验证样本；
训练输入归一化后为 690 种。验证准确率相同时，用验证损失选择模型。

| 开发回归指标 | 结果 |
| --- | --- |
| 原句留出集（216 条）严格结构 / 属性标签准确率 | 83.33% / 91.67% |
| 去掉与训练输入归一化重合的子集（90 条，仍有重复）严格结构 / 属性标签准确率 | 60% / 80% |
| 上述子集：预测属性属于合法多解集合 | 100% |
| 额外组合句属性检查 | 12 / 12 |
| 回归集合法功能词误压制率（带规则保护） | 0% |

该 surface holdout 已用于迭代开发，不是最终盲测集；归一化重复数和全部
错误样本在 JSON 报告内。高合法集合命中率不能替代严格结构学习指标。
“我还在 [MASK]”可以容纳动作、地点或语气词；模型可能只偏向动作，
因此用明确的粗语法允许集合保护其它合法词。该保护来自规则而非纯学习。

| 全词表补词题 | 主 MLM 原始刘邦排名 | 新过滤器后 |
| --- | --- | --- |
| 这位 高祖 叫 [MASK] 。 | 6 | 2 |
| 高祖 叫 [MASK] 。 | 10 | 4 |
| 高祖 的 原名 是 [MASK] 。 | 36 | 24 |

第一题前五：人、刘邦、他、王、汉王。原始排名是绕过旧耦合 gate 的
主 MLM logits，因此不应和之前带旧 gate 的排名混为同一个基线。

主 checkpoint SHA256 在训练和评估前后相同；主参数扰动后的过滤器输出
最大差为 0，独立模型保存恢复最大差为 0。六项行为测试覆盖梯度隔离、
padding、保存恢复、未标注词中性、多属性种子、分句隔离和歧义词保护。

当前仍不是通用中文语法解析器：种子之外的内容词保留 UNKNOWN，尚未
自动归纳人物、地点等属性，因此“刘邦”也可能被放行到地点槽位。
过滤器也不做跨分句指代推断。后续应优先补集合监督、独立盲测语料和
未标注词属性学习，再衡量是否值得继续训练主 BERT。

完整产物：`outputs/independent-grammar-filter/report.json`、
`training_log.json`、`dataset.json`、`grammar_filter.pt`。
