# 架构诊断与问题复盘日志（2026-09-16）

**文档主题**：关系候选空间爆炸、内存泄漏、12维玩具模型假象与“两两词对”破坏泛化性的根因复盘及整改方案
**相关代码与配置**：`bert_simple/structured_relation.py`, `bert_simple/dynamic_qk_model.py`, `outputs/bert-mlm-fact-memory-shiji-dynamic-qk-256/dimension_combination_map.json`

---

## 1. 现象与外部评估矛盾

在前序实验与评估中，出现了一系列严重自相矛盾的现象：
1. **模型容量与资源消耗严重背离**：主 BERT 模型仅 4 层、256 维，整机参数文件仅 15MB~25MB，但在训练事实关系适配器时却频繁遭遇数十 GB 的内存吞噬，直接导致 Linux 系统 OOM Killer 杀进程。
2. **“泛化差、纯记忆”的错误归因**：评估日志多次声称“模型缺乏泛化能力，只能机械记忆训练集硬事实，未见句式测试集准确率跌至 0%”。
3. **“训练几轮就过拟合崩溃”的虚假诊断**：在针对“去成分、看不同”的特殊 18D 关系 FFN（`[q, k, |q-k|, q ⊗ k]`）进行验证时，评估声称“仅训练几轮效果便急剧恶化，判定该 FFN 容易过拟合”。

经底层数据与工程代码的深度审查，**上述问题全部是由严重扭曲的底层实现导致的工程人祸，而非模型架构与数学设计的缺陷。**

---

## 2. 根因剖析一：14,501 个两两碎片化词对（把深度学习退化为死字典）

### 2.1 荒谬的生成逻辑
在检查 `dimension_combination_map.json` 时发现，系统内居然堆积了 **14,501 个三维候选空间**。
其生成逻辑根本没有把完整事件框架作为一个监督单位，而是把二元 token key
（部分旧脚本直接滑动 Bigram，事实脚本则组合 MASK/目标词与若干上下文词）映射成空间：
```python
# 致命的历史伪代码逻辑：
for i in range(len(tokens)):
    combination_map[f"{tokens[i]} || {tokens[i+1]}"] = [dim1, dim2, dim3]
```

### 2.2 垃圾噪声泛滥
经脚本直接统计，这 14,501 个空间中：
* **仅常见标点与停用词就强占了 4,357 个空间**（如 `"是 || 的"`, `"是 || 了"`, `"是 || ，"`, `"在 || 的"` 等）；
* 绝大多数其余空间也都是语料中只出现过一次的长尾偶发词对。
* **为虚词和标点分配专属的 3D 实体关系空间，完全背离了实体知识建模的初衷。**

### 2.3 语义截瘫：两两切碎抹杀了事实关系
真正的历史事实是高阶协同的完整事件框架（Event Frame）：
$$\text{Event} = (\text{施事: 汉王}, \; \text{动作: 封}, \; \text{受事: 张耳}, \; \text{结果: 赵王})$$

当算法将事件粗暴切碎为孤立的两两词对时：
* `("汉王", "封")`：汉王封赏过数十位开国功臣（韩信、彭越、英布、萧何），仅凭两词语义极度发散；
* `("封", "张耳")`：项羽曾封张耳为常山王，汉王曾封张耳为赵王，缺少主语根本无法消歧；
* `("为", "赵王")`：秦末汉初赵王有武臣、赵歇、张耳、张敖，缺少前序关系完全是无效信息。

**核心铁律**：**看关系，组合成员必须到达临界量，宁多勿少！** 语言事实具备高度的非线性协同性，切碎成孤立双词对直接导致信息在源头丢失，神仙也看不出事实。

### 2.4 泛化性被人工扼杀
正因为每个空间被硬编码绑定到特定的文字词对，一旦测试集换了代称（“高祖”替代“汉王”）或倒装调整了语序，在 14,501 字典中直接未命中（Cache Miss），测试准确率自然归零。
**并不是模型不能泛化，而是这种死板硬哈希字典从物理上阻止了模型泛化！**

---

## 3. 根因剖析二：广播核弹干爆内存（17GB+ 虚空消耗）

在 `bert_simple/structured_relation.py` 的初始实现中，为了计算序列内所有 token 在各个候选空间的关系偏置，采用了未经分块的全局高维广播：

$$\text{Tensor Shape} = [\text{Batch}, \; \text{Heads}, \; \text{Spaces (14,501)}, \; \text{Seq\_Len}, \; \text{Seq\_Len}, \; 18]$$

以常规超参（$\text{Batch}=1, \text{Heads}=4, \text{Spaces}=14,501, \text{Seq\_Len}=64$）计算：
$$\text{Size} = 1 \times 4 \times 14,501 \times 64 \times 64 \times 18 \times 4 \text{ bytes} \approx \mathbf{17.1 \text{ GB}}$$

在 PyTorch 反向传播中，Autograd Graph 还需要暂存前向激活值，瞬间内存需求突破 **35GB ~ 50GB**，直接将物理机器的内存打爆。

用户及时提出的**“1GB 军规三原则”**（查广播改矩阵乘、查切片按 512/1024 chunking、查梯度对 bank 加 no_grad）是排查此问题的唯一正解。

---

## 4. 根因剖析三：12 维玩具模型与“容量饿死”假象

为了应对内存爆炸，此前的验证流程采取了极端的“掩耳盗铃”策略：
* 将隐层维度从正常的 256 维直接砍成 **12 维**；
* 将路由维度砍成 **4 维**；
* 每个注意力头的维度仅剩 $12 / 2 = 6$ 维；
* 候选空间砍到仅剩 2 个 `[(0, 1, 2), (3, 4, 5)]`（见 `test_structured_relation.py`）。

### 4.1 维度饥渴导致表征对撞（Representation Collision）
* 12 维空间最多仅能容纳 **12 个互相正交的方向**；
* 当把多个复杂的历史事实塞入 12 维模型时，反向传播为了拟合当前样本，只能**暴力擦除和覆写**前一个样本占用的维度；
* 几轮训练下来，不同事实的梯度在高维空间剧烈对撞冲突（Gradient Conflict），导致测试集指标断崖式下跌。

**评估将这种“维度极度饥渴导致的容量崩溃”误判为“特殊 FFN 容易过拟合”，完全是倒果为因。**

---

## 5. “去成分看不同”特殊 FFN 的理论正名

用户设计的特殊 18D 关系特征：
$$\text{Features} = [q, \; k, \; \underbrace{|q - k|}_{\text{去共有成分，抓残差差异}}, \; \underbrace{q \otimes k}_{\text{高阶双线性交互}}]$$

在数学表征理论中具有高度先进性：
1. **TransE 与度量学习本质**：实体关系即向量残差偏移（$h + r \approx t \implies r \approx t - h$）。差值 $|q - k|$ 扣除了两者的公共上位范畴属性（例如“汉王”与“张耳”都是历史人物、“常山王”与“赵王”都是王爵），迫使 FFN 聚焦于两者的本质区别。
2. **高阶协同**：外积 $q \otimes k$ 提供了多项式交互能力，使得 FFN 具备判断主谓宾组合合法性的非线性门控能力。
3. **反例第一击的非对称排斥力**：
   在交叉熵损失 $\mathcal{L} = -\log(P)$ 下：
   * 正例重复多次，$P=0.99 \implies \text{Loss} \approx 0.01$（边际收益递减，符合 $\log$ 平缓区）；
   * 突现强反例（Hard Negative），$P=0.01 \implies \text{Loss} \approx 4.60$（**产生 460 倍强度的反冲梯度！**）；
   * 极少数高价值硬反例能瞬间撕开判定边界（Margin），防止表征空间引力塌缩。

---

## 6. 后续架构整改规范与红线

### 规则一：全景上下文输入，严禁局部切碎
* 坚决废除任何相邻词对（Bigram）的硬编码哈希映射。
* 模型输入必须保持完整的事件上下文（包含主语、谓语动词、宾语、修饰成分），依赖主 BERT 隐层编码提供全局语义，宁多勿少。

### 规则二：固定共享上下文 bank，禁止按关系扩容
* 三维 space 是可复用的实现通道，不是事实类别或 token-pair ID。256 维主 hidden 当前固定使用 1500 个共享候选；前 86 个基础三维通道覆盖全部维度，其余候选不绑定任何词对。
* 关系复杂度不通过新增空间表达，而由每个位置自己的 contextual hidden、route 分布和完整上下文组表达。
* 新事实只能改变共享 bank 的参数和特殊 FFN 的边界；不得因为新词、相邻词对或梯度冲突再创建空间。

### 规则三：严格落实 1GB 内存预算
* 计算 $R_{ij}$ 关系分数时，严禁构造 `[B, H, Spaces, T, T, 18]` 全局广播大张量。
* 关系 sidecar 训练时只计算 MASK 与完整关系上下文组/完整背景组的受监督交互；组内先聚合全部成员，再进入特殊 FFN，不能用 `topk` 或逐词对替代关系组；
* 静态 bank 构建必须处于 `torch.no_grad()` 上下文，彻底斩断无关计算图。

### 规则四：实验必须基于真实容量（256 维）
* 12 维与 4 维仅限用于毫秒级执行的 CI/Smoke 单元测试（仅检查代码语法和张量形状连通性）；
* 凡涉及“泛化性、学习率、过拟合、事实记忆”的所有实验与分析，必须统一在 **256 维、4 层、中间层 1024** 的标准尺度上进行。
