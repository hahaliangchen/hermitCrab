# BERT 训练路线

## 当前目标与执行顺序

当前优先级已经调整。生成式问答暂时后置，先把独立的语法结构和词属性
过滤器训练到能在真实史记句式上泛化，再用冻结的过滤器辅助主 BERT 重训，
最后才进入生成式问答。

执行顺序是：

1. 准备大量、句级、分词后的《史记》原料；
2. 用合成结构和真实史记弱监督训练独立的 8 维 `g_t`、8 维 `h_t` 与独立 Q/K；
3. 在按篇章留出的真实盲测集上验证语法槽位、属性和误过滤率；
4. 冻结过滤器，使用它辅助重训主 BERT，并与没有过滤器的基线对照；
5. 重新训练或校准事实记忆和三维动态 Q/K；
6. 生成式解码器最后实施。

详细的数据要求、结构覆盖和划分规则见 [GRAMMAR_DATA_PLAN.md](GRAMMAR_DATA_PLAN.md)。

项目当前只有 `examples/shiji_baihua_zhangchen_gaozu_long_context.txt` 这份
100 行节选，约 8920 个空格分词 token，不是完整《史记》。合成语法样本
可以先用于开发，但不能代替大规模真实史记原料。

## 阶段一：通用 MLM 上下文学习

训练样本使用连续原文合并的长上下文块，目标长度约 200 个词元，最大长度设为 256（含特殊 token）。
样本不足或超长时不静默截断；每个 epoch 遍历全部样本，并重新生成遮罩。

建议配置：

- hidden size：256
- Transformer 层数：4
- 注意力头数：4
- 学习率：`2e-4`
- 训练轮数：20～30
- MLM 遮罩概率：`0.15`
- 每个样本都保留 `[CLS]` 和 `[SEP]`
- 位置表示使用有方向的相对位置 bias，不使用绝对位置 embedding；超出距离范围的 token 使用边界距离桶

三维组合在第一轮建立后固定，后续 epoch 不重新分配。这里采用“软隔离”而不是完全冻结：当前关系使用的三维组合获得完整更新，不相关维度仍允许较小比例的共享更新（例如 10%～35%）。这样可以保留 Transformer 的全局适应能力，也不会因为一次疑似冲突就浪费大量维度。

软隔离的判定规则：

1. 新关系先尝试使用现有三维组合，并保留旧关系的回放梯度。
2. 如果新旧梯度只是轻微冲突，继续允许共享更新，只做小幅衰减。
3. 只有当同一关系连续出现明显冲突，且旧关系的事实遮罩准确率或 logits 明显下降时，才降低共享更新比例，或新增局部三维组合。
4. “新增组合”表示增加可用的局部参数容量，不表示把整个旧模型重排；旧组合仍可参与推理。
5. 评估时同时检查共享更新造成的漂移，不能只看梯度余弦或通用 MLM loss。

因此，三维组合的目标是“优先保护、允许小幅改变、严重冲突再隔离”，而不是把每个关系硬切成互不相干的参数块。

## 阶段二：稀有事实记忆

通用 MLM 的随机遮罩会导致低频事实很少被当作目标。例如“魏国”只出现几次，即使训练 20 轮，也可能只得到少量有效监督。因此增加一个小的事实记忆阶段：

1. 扫描词频，找出低频且具有中文内容的候选词。
2. 对每个候选词的每个原文出现位置，单独建立一个训练样本。
3. 强制把该词替换为 `[MASK]`，其余上下文保持可见。
4. 重复训练这些事实样本若干轮，让模型反复学习“上下文 → 稀有事实词”。
5. 使用较小学习率，例如 `5e-5`，同时回放通用 MLM 样本，避免事实记忆覆盖普通语言能力。
6. 三维组合映射保持不变，不因事实阶段新增组合。

事实阶段同样采用软隔离。事实样本可以改变共享的 Embedding、Q/K/V、FFN 和输出层，但必须保留一组旧事实锚点进行回放。对锚点记录旧模型的目标词概率、Top-1 / Top-5 和隐藏表示；如果新事实训练让旧锚点明显下降，再降低该更新在共享维度上的比例。高频事实（例如已经多次学到的“高祖 → 刘邦”）也要进入锚点集，不能只保护低频词。

需要注意：仅仅把梯度乘上三维掩码并不能保证关系完全不变。AdamW 的动量、共享 bias、跨层残差和非线性都会产生关系漂移，所以事实阶段应使用单独的事实保持指标；“遗忘率为 0”不能替代对具体事实问法的复测。

事实阶段必须单独评估：

- 固定事实遮罩的 Top-1 / Top-5 准确率；
- 事实样本的 MLM loss；
- 通用样本的记忆损失和遗忘率。

遗忘率只能说明旧能力有没有被破坏，不能代替事实答案准确率。

当前事实数据构建入口是 `examples/build_shiji_fact_dataset.py`。它扫描仓库现有的
`data/shiji/segmented/shiji_segmented.txt`（当前 10,631 行），输出结构化事实清单和
报告。事实清单除了任命、攻伐、战果、籍贯，还包含标题—人物、别名、本名/字号、
父子、祖孙、兄弟、婚姻和世系边；每条关系保留规范的 `relation_type`、方向、双方
实体及 `source_line`，同一源行固定划分到同一 split，避免长上下文变体泄漏。

这表示当前仓库语料已经被批量扫描，不表示所有章节的关系都已经人工核验完成。
报告中的关系覆盖率只统计命中明确词典/句式规则的源行；分词粘连、代词缺少同句
先行词或语义需要跨段落判断的关系仍会被保守跳过，训练前应继续做事实盲测和噪声
抽检。

## 三维动态 Q/K 路由（当前采用方案）

三维通道不是事实 ID，也不是 token 两两组合的注册表。动态三维 Q/K 的职责是给
上下文注意力提供可复用的附加通道；关系事实由每个位置自己的 contextual hidden、
完整上下文组和特殊 FFN 共同判断。

当前方案是“固定共享 bank + 位置级可学习 route”：

1. `build_context_space_triples(256, candidate_count=1500)` 建立 1500 个固定共享三维候选；其中前 86 个基础通道覆盖全部 256 个 hidden 维度，其余候选提供额外可复用容量；不按 token、词对、事实或句子分配新空间。
2. 每个 space 维护可学习的 `3×3 Q/K` 和 route descriptor；它们是共享容量，不携带具体词义标签。
3. 第 1 层普通相对位置 BERT 为每个位置产生自己的 `h_t`；route query 从这个 `h_t` 计算，route key 从共享 descriptor 计算。
4. 同一句中两个相同 token 也会因各自的上下文和相对邻接不同而得到不同 route；route 不存进 attention 的可变状态。
5. attention 内部可以计算 token 对的局部 logit，但关系 sidecar 的训练单位是完整上下文组：先聚合全部关系成员，再由特殊 FFN 判断 MASK 与该组是否匹配。
6. 动态 bank 按 `layer × head × shared-space` 保存三维参数，使每个 attention head 都有自己的 local score；不再保存或读取旧的 `dimension_combination_map.json`。
7. 旧的冲突拆分/词对分配脚本只作为历史对照，不是当前训练入口；新的 `examples/train_full_relation_filter_stages.py` 按 FFN → route → Q/K 三阶段训练。
8. 动态模型额外保存 `dynamic_model_config.json`，`DynamicWordSpaceBertForMaskedLM.from_pretrained()` 可以自行重建 bank 和 route 配置。
9. BERT 的位置关系使用相对位置 bias，而不是把绝对位置向量加到 token embedding；因此 route 输入看到的是上下文和相对距离，不会把句子整体偏移当成新的绝对语义。

### 语法结构与词语属性层

动态三维 route 之前保留一层普通 BERT 作为 grammar/context layer。它为每个位置产生独立的 contextual hidden，后续的 route Q/K 和词属性 gate 都使用这个位置自己的表示，不把同一句中的相同 token 当成同一个状态。

语法部分只写粗粒度结构允许集合，不写死答案。例如：

- `建都/定都在` 后优先保留 `PLACE/ENTITY/UNKNOWN`；
- `本名/又称/是/为` 后优先保留人物或实体；
- `有/约有` 后优先保留数量或实体；
- `在` 是歧义触发词，允许地点、时间、实体、功能词、疑问词和未知词，避免把“我在呢”误判成只能接地点；
- `在 + 地点 + [谓词]` 使用单独的谓词槽约束；条件、原因、并列等只提供粗结构先验。

词属性不是完整人工词典。训练只给标点、在/去/的/和/是/哪等功能或疑问词，以及少量 `县/郡/城/地/人/王/年` 等内容锚点；其余词从 `UNKNOWN` 开始，通过 MLM 梯度和上下文槽位对齐学习。已知锚点在不兼容的 grammar 槽位中做强降权，未标注内容词仍使用 soft compatibility，避免错误硬过滤。

每个训练输出还保存 `word_attribute_registry.json`，其中记录属性名、种子词及 grammar role；训练日志额外记录 `slot_entropy` 和 `target_attribute_compatibility`。

路由和内容 Q/K 分成两层：

```text
上下文 hidden → route QK → 候选 space 权重 α
                             ↓
                    space-specific 3D Q/K
                             ↓
                    dynamic local attention score
```

当前使用相对位置 BERT 的第 1 层输出作为路由输入，再从第 2 层开始加入动态三维 Q/K。这样同一个词在不同位置会根据各自的上下文得到不同的路由，同时避免“必须先有动态路由才能计算产生路由所需 hidden state”的循环依赖。

训练期间需要保存旧事实锚点的路由分布，并加入路由保持约束，例如 `KL(alpha_old || alpha_new)`，避免事实训练导致旧事实突然切换到另一套空间。路由指标还应记录候选数量、top-1/top-k 命中率、space 使用频率和路由熵，防止所有 token 坍缩到同一个 space。

原始动态 MLM 实验入口是 `examples/bert_mlm_dynamic_word_spaces.py`，默认使用项目已有的 `examples/shiji_baihua_zhangchen_gaozu_long_context.txt`，从随机初始化开始训练，不依赖旧 checkpoint。关系 sidecar 的当前入口是 `examples/train_full_relation_filter_stages.py`：它使用固定共享 bank、当前位置 contextual hidden 和完整关系上下文组，在特殊 FFN → route → 动态 Q/K 三阶段训练；旧的 token-pair allocator 与 `train_shiji_fact_memory_dynamic_qk.py` 仅保留作历史对照，不能作为当前泛化训练入口。

为控制事实分支的参数规模，新主线的 Q/K 矩阵直接作用于固定共享 bank 指定的三个 hidden 维度，形状为 `layer × head × shared-space × 3 × 3`；它保留逐 head 动态 Q/K 语义，但没有复制旧 bank 中完整 hidden-to-3D 的 `P_q/P_k` 投影，也不把某个事实绑定到某个坐标三元组。

## 后置阶段：生成式问答

本阶段暂不优先实施。只有独立语法过滤器在真实史记盲测上稳定，且主 BERT
在过滤器辅助重训后事实 Top-1/Top-5 和遗忘率都有明确对照结果，才进入
生成式训练。这样生成式模块使用的是已经稳定的上下文和事实表示。

在 BERT 编码器之上加入生成式解码器：

```text
问题 + 史料 → BERT 编码器 → 上下文向量
                         ↓ Cross-Attention
                  因果解码器逐词生成答案
```

解码器训练需要问题—答案样本，使用 teacher forcing 和 next-token loss。解码器自身必须使用因果 Mask，不能看到未来答案。三维组合可以继续用于编码器关系更新和解码器的 Q/K 更新。

## 评估原则

### 当前优先阶段：独立 8+8 维语法过滤器

保留现有主 BERT checkpoint。`examples/train_independent_grammar.py` 只训练
`bert_simple/grammar_filter.py`：独立 embedding → 两层小型相对位置 attention →
8 维结构 `g_t` / 8 维属性 `h_t` → 结构分类头与属性 Q/K。
两个分支也读取当前位置前后各一个 token 的独立 embedding，保留局部结构
线索，避免远处分句覆盖命名或动作触发词；这只是相对邻接输入，不使用
绝对位置。训练包含句末语气词变化以及分句组合。
独立过滤器的 attention 用逗号、句号、问号建立粗分句范围，限制无关
分句干扰；主 BERT 仍可读取完整上下文。该粗范围不处理跨分句指代消解。
“到了哪/到了吗”等多解句式另外报告合法属性集合命中率，并保留原始
单一模板标签准确率，避免把合法用法当作错误或隐去原始指标。
这里的动态性指 query 随当前位置上下文变化，投影与属性 key 是训练参数；
不意味着每句话生成一套新的矩阵参数，也不改主模型的三维空间 route。

结构监督覆盖命名、位置、时间、动作、宾语、身份、数量、条件、并列、助词、
句界与疑问。时间例子保留“时/期间”等证据，避免错误地认定“在…出发”
只能接时间。“我在呢”和“在哪”分别纳入功能词与疑问属性测试。
这些集合是粗粒度实验覆盖，不是穷尽中文句法。

属性 key 用槽位监督学习，候选词属性表用少量现有种子监督；未标注词保持
UNKNOWN，中性放行。当前优先验证结构，不把未知词归类效果当作已完成。
结构层优先训练并不会自动降低主 BERT 的学习难度：后续应单独比较固定
过滤器下的事实排名、MLM loss 和误过滤率，决定是否恢复主流程训练。

每次训练保存 train/validation/test 样本；surface test 保留不同句式与主语，
已用于方案开发回归，不能称为未查看过的盲测集，且不参与 checkpoint 选择。
输入内容词归一化及结构同义词共享会造成原句之间重复，另报归一化后未见
样本与组合句表现。训练加入目标位置前后的无关分句，避免把逗号当作条件
从句的唯一依据。报告结构准确率、属性准确率、功能词/标点压制率、合法
功能词误压制率，以及原始/过滤后的全词表排名。检查主模型文件 SHA256、
主权重扰动后的过滤器输出不变、独立模型保存恢复一致。

每次实验同时记录：

- 通用 MLM loss；
- 稀有事实 Top-1 / Top-5；
- 已知事实锚点的排名变化，例如“高祖，本名 `[MASK]`”中的“刘邦”；
- 生成式答案的完整匹配率；
- 遗忘率；
- 实际使用的三维组合数量。

只有“答题准确率”和“生成质量”提升，才能说明模型真的学会了；单独降低 loss 或遗忘率还不够。
