# 三维关系推理与结构化 FFN 设计思路

## 1. 目标

本项目真正希望获得的能力，不是让模型保存“某个 token 曾经和哪些 token 一起出现在句子中”，而是让模型在推理时能够主动完成两种操作：

1. **找不同**：比较结构相似的对象，分离共同成分与差异成分，判断差异带来了什么结果。
2. **去成分**：移除一个 token、维度或关系成分，重新计算结论，观察预测发生了什么变化。

句子共现记录可以作为训练样本、辅助监督或事实记忆，但不应成为主训练和主推理机制。主推理应依靠可组合的关系规则完成。

## 2. 当前动态 Q/K 的意义

当前动态 Q/K 的特殊设计包括：

- 上下文路由器根据当前 hidden state 选择候选空间；
- 每个 layer/head/space 拥有独立的三维 Q/K 变换；
- 三维 Q/K 经过组合后生成局部 attention score；
- 局部分数与普通 BERT attention 分数相加。

因此动态 Q/K 不只是增加参数，而是给模型加入了“先选择关系空间，再判断 token 之间关系”的归纳偏置。这是它在低数据条件下比普通模块更有潜力的原因。

动态 V 的实验没有带来稳定收益，所以当前设计暂不把动态 V 作为重点。普通 V 已经可以提供被搬运的内容，当前更值得研究的是如何分析关系和成分。

## 3. 关系不应表示成句子列表

最直观但不理想的表示是：

```text
张耳 → 汉王、立、赵王、……
```

这种表示本质上是原始共现记忆：能够记住见过的句子，但不自然、不灵活，也难以泛化到未见过的组合。

更理想的表示是学习一个关系函数：

```text
三维状态组合 → 关系规则 → 关系分数或关系类型
```

模型保存的是关系规则和结构化参数，而不是每条原始句子。

## 4. 三维关系特征

对于当前 token `i` 和被关注 token `j`，在某个动态空间 `s` 中定义：

```text
q(i,s) = [q1, q2, q3]
k(j,s) = [k1, k2, k3]
```

不只使用普通点积，还可以显式构造：

```text
共同/协同成分：q ⊙ k
差异成分：|q - k|
所有跨维组合：q ⊗ k
```

其中 `q ⊗ k` 是一个 `3×3` 矩阵：

```text
q1×k1  q1×k2  q1×k3
q2×k1  q2×k2  q2×k3
q3×k1  q3×k2  q3×k3
```

这样可以表达：

- 第 1 维相近，但第 2 维差异明显；
- 第 1 维和第 3 维同时激活；
- `q` 的第 2 维与 `k` 的第 3 维形成关系。

## 5. 结构化关系 FFN

关系特征送入专门的关系 FFN：

```text
Φ(q,k) = [q, k, |q-k|, q⊗k]
relation_score = RelationFFN(Φ(q,k))
```

一个初始原型可以是：

```text
18 → 32 → 1
```

这里的隐藏单元可以学习不同的组合检测器，例如：

```text
第 1 维相同 + 第 2 维不同
q2×k3 较强
第 1、3 维同时出现
```

最终关系分数可以写成：

```text
new_attention_score
    = ordinary_score
    + dynamic_qk_score
    + relation_score
```

由于当前 `a_q` 和 `a_k` 已经能够形成三维双线性关系，新的 FFN 必须提供真正的非线性交互，不能只是重复一次线性投影。

## 6. “找不同”的表示

找不同本质上是比较两个关系状态：

```text
difference = RelationState_A - RelationState_B
```

例如比较：

```text
汉王 立 张耳 为 [MASK]
汉王 立 韩信 为 [MASK]
```

共同部分可以表达通用的任命结构，差异部分可以表达人物与最终职位之间的具体关系。

关系比较可以使用：

```text
[state_A, state_B, state_A-state_B, |state_A-state_B|]
```

再交给差异 FFN，输出差异关系或候选结果变化。

## 7. “去成分”的表示

去成分是一种反事实操作。完整输入产生一个结果，去掉某个成分后再次计算结果：

```text
full_score = F(full_state)
drop_score = F(state_without_component_i)
impact_i = full_score - drop_score
```

对句子：

```text
汉王 立 张耳 为 [MASK]
```

可以分别去掉或软屏蔽：

```text
汉王：行为主体/权力来源
立或封：任命动作
张耳：被任命主体
为：职位槽位信号
```

`impact_i` 表示成分对“赵王”预测的促进或抑制作用。

初期应优先做三维坐标或关系成分的软消融，而不是对每个 token 做完整的 leave-one-out。这样计算量更低，也更接近结构化推理。

## 8. 推荐的整体结构

```text
hidden state
    ↓
动态路由，选择三维关系空间
    ↓
生成 q/k 三维状态
    ↓
构造共同、差异、跨维组合特征
    ↓
关系 FFN 分析组合规则
    ↓
attention 选择上下文
    ↓
成分消融分支，比较去掉因素后的变化
    ↓
残差 + LayerNorm + MLM 预测
```

这个结构的重点是：

```text
Q/K：决定比较谁、属于哪个关系空间
关系 FFN：判断三维组合代表什么关系
消融分支：判断每个成分对结论的影响
V：继续使用普通内容通道
```

## 9. 重要限制

三维坐标是潜在坐标。没有额外约束时，模型可以旋转整个坐标系，因此“第 1 维”不一定永远具有人类可解释的固定含义。

如果希望三条轴具有稳定语义，可以考虑：

- 稀疏或非负坐标；
- 少量轴语义种子；
- 轴遮蔽辅助任务；
- 维度多样性或正交约束。

此外，关系规则不能凭空产生具体事实。模型可以学习“某种任命结构如何组合”，但“张耳对应赵王”这条事实仍必须以参数、词向量或结构化记忆的形式存在。区别在于：保存的是可组合的事实和规则，而不是原始句子清单。

## 10. 2026-09-15 验证结果

当天没有修改 `bert_simple/model.py` 或默认动态 Q/K 主模型，只进行了验证性实验，并新增了独立实验脚本。

### 动态 V

在 100 行史料、80/20 训练验证划分下：

```text
动态 Q/K：             valid_loss=6.9045，valid_top1=11.7%
动态 Q/K + 动态 V：    valid_loss=6.8973，valid_top1=10.6%
```

差异很小，不能证明动态 V 能提高低数据泛化能力。

### FFN 变体

在相同数据和动态 Q/K 下：

```text
标准 FFN：             valid_loss=6.9045，valid_top1=11.7%
缩小 FFN：             valid_loss=6.8989，valid_top1=11.0%
去掉 GELU：            valid_loss=6.8922，valid_top1=11.7%
去掉 FFN：             valid_loss=6.8597，valid_top1=12.5%
```

这说明普通的“扩大维度 + GELU”并没有自动带来低数据优势；在这个未充分训练的小模型中，去掉 FFN 甚至略好，可能是减少了过拟合或优化负担。

并行 FFN 的结果与标准 FFN 完全一致，是因为简单的“分支后拼接”在数学上等价于一个更宽的普通 FFN，并没有真正产生专家分工。

这些结果不是对所有模型的普遍结论，而是当前项目的方向性证据：下一步应研究带动态路由、显式维度组合和反事实消融的结构化关系 FFN。

## 11. 下一步最小原型

1. 保留当前动态 Q/K，移除动态 V 实验路径。
2. 在动态局部分数旁增加共享的 `RelationFFN(18 → 32 → 1)`。
3. 使用 `q⊗k` 显式提供九种跨维组合。
4. 增加三维坐标的软消融和影响值记录。
5. 主要使用组合未见过的验证集，而不是只使用随机切分，检验真正的关系泛化能力。

