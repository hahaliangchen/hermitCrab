# 模型设计评审记录（2026-09-15）

本文记录当前架构中已经识别、但尚未完全解决的五个关键问题。它们是后续实验
和代码演进的约束条件，不能仅凭当前训练集结果视为已经解决。

## 1. 3×3 关系矩阵还不是完整的动态 Q/K 注意力

当前局部 3×3 关系矩阵主要在 MLM 输出端增加局部关系分数。它能够帮助事实词
与 hard negative 拉开 margin，但还没有改变 Transformer 注意力内部的 Q/K
计算流程。

真正的动态 Q/K 需要：

```text
当前位置的上下文 hidden
        ↓
route query
        ↓
候选关系空间匹配 / softmax 路由
        ↓
选择一个或多个关系空间
        ↓
在对应空间中生成局部 Q/K
        ↓
参与注意力分数计算
```

后续要确认同一个词在不同上下文中是否能够选择不同关系空间，同时避免所有词
坍缩到同一个空间。需要记录候选数量、Top-k 路由命中率、路由熵和空间使用频率。

## 2. 语法层和词语属性层尚未完全进入主 BERT 训练闭环

目前语法结构和词语属性已经可以独立训练、过滤和评估，但还没有完整地参与主
BERT 的隐藏状态学习与最终 logits 计算。

当前状态更接近：

```text
主 BERT → 上下文 logits
语法/属性层 → 独立先验或过滤信息
```

目标状态应为：

```text
主 BERT hidden
      + 语法结构兼容度
      + 词语属性兼容度
      + 局部事实关系分数
      → 最终 logits
```

接入时应优先使用可校准的软偏置，而不是过于激进的硬屏蔽。还要验证语法和
属性证据是否真的降低了标点、功能词的错误排名，而不是只让 loss 下降。

## 3. SGD 版本尚未经过真实训练验证

当前已经新增 SGD + momentum + Nesterov 的局部关系训练入口，但尚未启动训练。
因此现在只能确认代码入口和参数设计存在，不能声称 SGD 比 AdamW 更好。

正式对照必须固定：

- 初始 checkpoint；
- 数据划分；
- 随机种子；
- 事实样本、epoch 和 repeats；
- margin 参数；
- 局部学习率和全局梯度门控。

比较指标至少包括事实 Top-1 / Top-5、事实 loss、hard-negative margin、通用
记忆损失、遗忘率，以及全局路径和局部关系路径的参数更新量。

## 4. 局部关系分数可能过度主导全局路径

当前 v2 使用关系分数尺度 `24.0`。这有助于让局部关系在事实记忆中产生明显
作用，但也可能让局部适配器压过 BERT 的上下文判断。

后续要分别记录：

- 全局 base logits 的绝对值和局部关系 logits 的绝对值；
- 语法、属性、词频和局部关系的贡献比例；
- 事实正确率是否提升但普通 MLM 或新句式泛化下降；
- 局部关系矩阵是否出现异常增大或单一关系独占输出。

必要时增加局部分数温度、范数约束或逐步 warm-up，而不是继续盲目提高尺度。

## 5. 训练集事实命中不等于新句式泛化

目前部分“汉王—张耳—赵王”的手工测试句子来自训练样本。它们可以证明事实
记忆路径和局部关系分数正在工作，但不能证明模型掌握了未见问法或跨篇章推理。

后续评估必须增加：

1. 按篇章留出的事实测试集；
2. 同一事实的不同词序、不同句式和更长上下文；
3. 同篇章泛化与跨篇章泛化的分开统计；
4. 训练事实 A 更新后，对其他旧事实 B 的排名变化矩阵；
5. 事实记忆准确率与通用 MLM 遗忘率的联合报告。

只有留出集上的 Top-1 / Top-5、margin 和抗遗忘结果同时稳定，才能说明局部
三维关系机制不仅是在背诵训练样本。

## 当前优先级

```text
P0  SGD / AdamW 公平对照
 ↓
P1  按篇章留出的泛化与事实干扰矩阵
 ↓
P2  将语法/属性软偏置接入主 logits
 ↓
P3  实现上下文驱动的动态关系空间路由
 ↓
P4  在底座稳定后接生成式问答解码器
```

在 P0 和 P1 完成前，不把当前训练集事实结果宣传为泛化能力；在 P2 和 P3
完成前，也不把当前局部输出加分层称为完整的动态 Q/K 注意力。
