# 结构化三维关系 FFN 与因果差分推理架构设计
(Structured 3D Relational FFN & Counterfactual Differential Reasoning)

## 一、 核心背景与动机：从“死记句子”到“关系代数推理”

在自然语言与知识图谱的结合探索中，最直观却最粗糙的做法是：**让每个 Token 记录所有与它共现过的 Token 或历史句子**。

这种“倒排索引/硬编码记忆”的做法存在根本性缺陷：
1. **死板僵化**：无法泛化到未见过的表达方式或句式变换；
2. **缺乏推理能力**：模型只是在查字典，而不是在理解关系；
3. **参数爆炸且不优雅**：随着文本增长，索引表无限膨胀。

**用户提出的全新破局方向**：
> **抛弃动态 V 和死记句子的路线，把注意力转向“如何分析维度关系”——让 FFN 显式分析三维几何组合（如“第 1 维相同但第 2 维不同”、“第 1 维与第 3 维同时出现时才成立”、“q 的第 2 维与 k 的第 3 维形成跨维关系”），并使模型原生具备“找不同”和“去成分”的能力。**

---

## 二、 核心机制一：传统 Transformer 点积注意力的先天缺陷

标准 Transformer 的自注意力计算依赖向量点积：
$$\text{Dot Product} = q \cdot k = q_1 k_1 + q_2 k_2 + q_3 k_3$$

点积机制存在致命的**“对角线盲区”**：
1. **只能度量相同维度的同向对齐**（1 碰 1，2 碰 2，3 碰 3），最后粗暴加和为一个单一标量；
2. **完全丢失了跨维度交互（Cross-term Interactions）**：它根本无法表达“q 的身份轴与 k 的动作轴结合”；
3. **无法表达非对称逻辑**：无法表达“维度 1 必须相同，但维度 2 必须不同”的条件排他逻辑。

---

## 三、 核心机制二：21 维结构化关系特征构造器 $\Phi(q, k)$

设当前 Token 在三维空间的状态为 $q = [q_1, q_2, q_3]^T \in \mathbb{R}^3$，被关注 Token 的状态为 $k = [k_1, k_2, k_3]^T \in \mathbb{R}^3$。

为了将两个实体之间的**自身状态、差异、共性、跨维交叉**全部显式展开，构造 21 维的特征向量：

$$\Phi(q, k) = \Big[ q, \quad k, \quad |q - k|, \quad q \odot k, \quad \text{vec}(q \otimes k) \Big]$$

### 维度详细拆解（共 21 维）：
1. **自身坐标状态** $q$（3 维）：$[q_1, q_2, q_3]$ —— 发起者的当前三维属性；
2. **目标坐标状态** $k$（3 维）：$[k_1, k_2, k_3]$ —— 接受者的当前三维属性；
3. **找不同（差值绝对值）** $|q - k|$（3 维）：$[|q_1 - k_1|, |q_2 - k_2|, |q_3 - k_3|]$ —— 刻画两者的距离与相异度；
4. **找相同（同维相乘）** $q \odot k$（3 维）：$[q_1 k_1, q_2 k_2, q_3 k_3]$ —— 刻画在同一轴上的共振强度（逻辑与门）；
5. **跨维交叉组合（外积九宫格）** $q \otimes k$（9 维）：
   $$\begin{bmatrix}
   q_1 k_1 & q_1 k_2 & q_1 k_3 \\
   q_2 k_1 & q_2 k_2 & q_2 k_3 \\
   q_3 k_1 & q_3 k_2 & q_3 k_3
   \end{bmatrix} \xrightarrow{\text{展平}} 9 \text{ 个交叉分量}$$

> **极简优化变体**：
> 由于 $q \odot k$ 正好是外积九宫格的对角线，若剔除冗余，特征可精简为 **18 维**；若仅保留相对关系，甚至可精炼至 **12 维**。

---

## 四、 核心机制三：关系 FFN 与计算拓扑位置

### 1. 为什么 Relation FFN 必须放在 Attention Score 生成阶段？
- **错误位置**：如果放在 $A \times V$ 之后，所有的 Token 特征已经被加权平均混合成一团糊，模型根本无法分辨“究竟是哪两个词的哪个维度在交互”；
- **正确位置**：放在 **Attention 打分（Logit）阶段**。在矩阵混合之前，直接作为注意力分数的“因果几何引导透镜”：
  $$\text{Attention\_Score}_{ij} = \text{Base\_Score}_{ij} + \text{RelationFFN}\big(\Phi(q_i, k_j)\big)$$

### 2. Relation FFN 的结构：微型但极度高效
因为输入只有极度凝练的 21 维，Relation FFN 只需要一个极轻量的两层 MLP：
$$\text{Input}(21) \xrightarrow{\text{Linear}} \text{Hidden}(16) \xrightarrow{\text{GELU}} \text{Output}(1)$$
参数量仅约 360 个浮点数，运算速度极快，完全不构成算力瓶颈。

---

## 五、 核心机制四：因果差分推理（找不同与去成分）

该架构赋予了模型两种原生的因果分析算子：

### 1. 找不同（Differential Comparison）
比较两个上下文或两个事实特征向量的差分：
$$\Delta \Phi = \Phi(\text{事实A}) - \Phi(\text{事实B})$$
直接分离出共同的主干骨架与产生分歧的关键维度。

### 2. 去成分（Counterfactual Ablation / Shapley Value）
在推理时进行显式的反事实干预测试：
- 完整状态推断得分：$S_{\text{full}} = \text{RelationFFN}(\Phi_{\text{full}})$
- 遮蔽第 $i$ 个三维维度后的得分：$S_{\setminus i} = \text{RelationFFN}(\Phi_{\setminus i})$
- **因果边际贡献**：
  $$\Delta_i = S_{\text{full}} - S_{\setminus i}$$

**业务价值**：
如果抽掉第 2 维后，预测“赵王”的分数出现**断崖式下跌**（如 $\Delta_2 > 5.0$），模型便可直接给出可解释性证据：*“该结论完全由第 2 维（分封动作轴）所决定，背景实体并非充要条件。”*

---

## 六、 与历史成果的传承与闭环

| 演进阶段 | 核心机制 | 遗忘率 | 事实 Top-1 | 特点 |
| :--- | :--- | :--- | :--- | :--- |
| **V1 全局掩码 (0.02)** | 全局单矩阵 + 梯度弱更新门控 | 19.14% | 37.0% | 调参代偿，单句多关系踩踏 |
| **V2 动态 3×3 矩阵** | 7,210 个独立 $3 \times 3$ 矩阵按需分配 | **3.46%** | **37.0%** | 物理隔离，外积的线性加权版 |
| **V3 结构化关系 FFN** | 21 维关系特征 + 局部 FFN + 去成分 | **理论趋近 0%** | **更高且可解释** | **由记忆走向因果差分推理** |

> **关键洞察**：V2 阶段大获成功的独立 $3 \times 3$ 局部矩阵，在数学上恰好是 V3 阶段外积 $q \otimes k$ 的线性特例！这证明了该演进路径具有高度的一致性与严密的自洽性。

---

## 七、 后续落地实施路线

1. **原型模块编写**：在 `bert_simple/` 中封装 `Structured3DRelationFFN` 模块（输入 $q, k \in \mathbb{R}^3$，输出标量分数与 21 维可解释性分析结果）；
2. **稀疏候选激活**：结合已跑通的 `IndependentGrammarFilter`，仅对主语、宾语等关键候选实体执行外积分析，跳过无关标点与虚词；
3. **去成分验证工具**：提供 `.explain_prediction(q, k)` 接口，直接输出各维度的消融因果贡献柱状图。
