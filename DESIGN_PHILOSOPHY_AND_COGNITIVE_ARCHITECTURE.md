# 认知双系统与稀疏流形门控：HermitCrab 设计思想与对话精粹

> **“语法说话，本就是习惯远超于思维。全注意力注意太多没有丝毫营养的东西，换成人如此瞎注意，那就是纯瞎学，努力感动了自己。”**  
> —— 本项目核心认知对话实录

---

## 目录
1. [背景与核心矛盾：全注意力的“勤奋假象”](#1-背景与核心矛盾全注意力的勤奋假象)
2. [双系统假说：System 1 习惯反射 vs System 2 几何流形推理](#2-双系统假说system-1-习惯反射-vs-system-2-几何流形推理)
3. [过拟合悖论的数学本源：Loss 作用域错配](#3-过拟合悖论的数学本源loss-作用域错配)
4. [内嵌式 If-Else：注意力稀疏 Mask 门控机制](#4-内嵌式-if-else注意力稀疏-mask-门控机制)
5. [子矩阵 18 维 FFN：算力与显存的百倍革命](#5-子矩阵-18-维-ffn算力与显存的百倍革命)
6. [零人工标注：基于既有三元组的弱监督自举](#6-零人工标注基于既有三元组的弱监督自举)
7. [三维拓扑与 1/3 维度融合的几何直觉](#7-三维拓扑与-13-维度融合的几何直觉)
8. [对话精粹与认知金句实录](#8-对话精粹与认知金句实录)
9. [技术演进全景路线图](#9-技术演进全景路线图)

---

## 1. 背景与核心矛盾：全注意力的“勤奋假象”

现代 Transformer 架构的核心基石是全双向自注意力机制（Dense Full Self-Attention）。在标准形式下，长度为 $T$ 的输入序列会生成大小为 $T \times T$ 的全连接注意力矩阵。当序列长度扩展至 135 到 256 时，模型在每一个层、每一个 Head 都要计算 $135 \times 135 = 18,225$ 个词对的交互。

然而，在人类的语言认知中，这一假设是严重失真的：
* **无营养的注意力浪费**：在古汉语或现代汉语中，诸如“是、的、为、在、以、之、者”等语法功能助词，以及逗号、句号等标点符号，占据了序列的大半篇幅。
* **“努力感动了自己”**：人类在理解“蔺相如 为 赵 舍人”时，大脑绝不会去理性推演“为”和“赵”之间有什么深奥的本体关系，更不会去建立“， 与 。”的逻辑联结。全注意力强行让模型去对全句所有排列组合做复杂的加权与梯度反传，相当于强迫学生去死记硬背一本书里每一个逗号和句号的相对距离。其结果是算力被海量垃圾点对耗尽，看似在剧烈计算，实则在做无用功。

在本项目 18 维高维关系 FFN 下，全注意力的缺陷直接引爆了物理瓶颈：单句 $18,225$ 个点对在 8 个空间展开时，瞬间吞噬 **10.6GB 显存**，推理与训练成本不可承受。

---

## 2. 双系统假说：System 1 习惯反射 vs System 2 几何流形推理

为了破除全注意力的盲目性，我们提出了**解耦语言习惯与逻辑推理的双系统认知架构**：

```
                              输入序列 (Tokens)
                                     │
                 ┌───────────────────┴───────────────────┐
                 ▼                                       ▼
    ┌─────────────────────────┐             ┌─────────────────────────┐
    │   System 1 (直觉与习惯)   │             │   System 2 (理性与流形)   │
    ├─────────────────────────┤             ├─────────────────────────┤
    │ · 浅层 (Layer 0, 1)     │             │ · 深层 (Layer 2, 3)     │
    │ · 全注意/因果局部单向    │             │ · 稀疏门控 (Sparse Gate) │
    │ · 词属性与句法骨架       │             │ · 8 空间 3D Q/K 路由    │
    │ · 允许且鼓励死记硬背     │             │ · 18 维立体几何 FFN     │
    │ · 解决“是后面跟的”       │             │ · 解决“实体身份与因果”   │
    └─────────────────────────┘             └─────────────────────────┘
                 │                                       │
                 └───────────────────┬───────────────────┘
                                     ▼
                          最终 Mask 答案预测与流形推理
```

### System 1：习惯、语感与机械反射（浅层）
* **认知定位**：对应卡尼曼《思考，快与慢》中的系统 1。负责语法语感、固定句式习惯、词法连贯。
* **特性**：极速、自动化、局部化。例如“我爱你”是“主-谓-宾”结构，“为……所……”是被动句。**对于这一层，我们巴不得它死记硬背**。过拟合语法句式并不是坏事，而是建立稳定的语言本能。
* **模型实现**：保留在 BERT 的浅层网络（Layer 0, 1），或者使用极轻量的属性过滤器。在这一层，注意力保持全局连贯流动，建立基础的上下文表示。

### System 2：几何流形、逻辑与排他推导（深层）
* **认知定位**：对应系统 2。负责真正的本体知识、角色槽位绑定、实体排他性逻辑。
* **特性**：高维抽象、精确受控、强几何拓扑约束。
* **模型实现**：在 BERT 的高层网络（Layer 2, 3）开启。该层严格禁止在废虚词对上浪费容量，唯有真正的候选实体对和 `[MASK]` 槽位，才允许进入 8 空间动态 Q/K 路由与 18 维非线性关系 FFN 进行深度流形计算。

---

## 3. 过拟合悖论的数学本源：Loss 作用域错配

在早期的实验中，模型展现出了极其极端的现象：
$$\text{Train Top-1 Acc} = 100\% \quad \text{vs} \quad \text{Dev Top-1 Acc} = 48.15\%$$

通过深入剖析，我们揭示了这一现象的数学本质：**不是 Loss 设计错了，而是 Loss 作用的目标错了**。

1. **强泛型 Loss 的初衷**：
   我们设计了结合 Margin Softplus、成对对比（Invariance/Contrast）以及候选交叉熵的强几何损失函数。这套 Loss 是专门为“实体-属性-实体”这种具备强拓扑不变性与排他性的组合关系设计的，其目的是在 18 维特征空间中拉开清晰的决策超平面。
2. **错配的灾难**：
   在全注意力下，这套高容量的 Loss 被迫作用在了一万多个荒谬的配对上（例如“是”对“的”，“为”对“者”）。
3. **退化为查找表**：
   因为虚词和标点之间的共现根本没有固定的本体关系，它们在不同语境下的组合纯属噪声。一个高容量的神经网络在面对噪声监督时，为了使 Loss 归零，唯一能做的事情就是**动用全部参数去硬背该样本中每个噪声词的具体位置与上下文特征**。
   模型最终蜕化成了一张“特定文本查找表（Lookup Table）”——在训练集上能分毫不差地答对，但遇到换了语境的验证集，虚词噪声一变，整个推理网络便瞬间坍塌。

---

## 4. 内嵌式 If-Else：注意力稀疏 Mask 门控机制

既然问题在于“废关系污染了深层网络”，解决方案就是加入结构化的逻辑控制：**在 Attention 矩阵中内嵌 if-else 稀疏门控**。

### 门控逻辑与数学形式
对于输入序列中的任意两点 $(i, j)$，门控模块先通过词属性判断该点对是否具备实体关系资格：
$$\text{Gate}(i, j) = \begin{cases} 
0.0, & \text{若 } i, j \in \{\text{实体, 称谓, 待测槽位}\} \\ 
-\infty \ (-10000.0), & \text{若 } i \text{ 或 } j \in \{\text{虚词, 助词, 标点}\} 
\end{cases}$$

将此门控矩阵直接加到深层自注意力分数上：
$$\text{Attn\_Scores}_{layer \ge 2} = \frac{Q K^T}{\sqrt{d_k}} + \text{Rel\_Dynamic\_QK} + \text{Gate}$$

经过 Softmax 之后：
$$\text{Softmax}(\dots + \text{Gate})_{i, j} = 0.0000 \quad (\forall (i, j) \in \text{废关系})$$

### 哲学意义：让 if-else 与神经网络优势互补
很多深度学习从业者执着于“End-to-End 全可微”，试图让全注意力自己去学会忽略标点。但正如我们在对话中总结的：
> *“只要它能过拟合死记硬背句式组合，后面加一个 if-else 机制进行过滤筛选，比让模型自己在大海捞针中瞎碰要清醒、高效得多。”*

门控机制相当于在神经网络的流形空间中设立了“交通隔离带”，严禁语义噪音干扰高阶推理。

---

## 5. 子矩阵 18 维 FFN：算力与显存的百倍革命

如果仅仅在 Attention Logit 上加 $-\infty$，计算图依然要分配 $135 \times 135 \times 18$ 的中间张量，显存爆炸问题依然未解。

真正的架构突破在于：**不仅掩码置零，更要切片降维（Submatrix Computation）**。

```
       全注意力外积空间 (135 x 135 = 18,225 点对)                稀疏子矩阵 (14 x 14 = 196 点对)
       ┌──────────────────────────────────────────┐             ┌────────────────┐
       │ ········································ │             │ 周  太  舍  MASK│
       │ ···· 是 ·············· 的 ·············· │             │ 亚  尉  人     │
       │ ········································ │  ──过滤──>   │ 夫             │
       │ ··········· 魏子 ··········· 蔺相如 ···· │             │                │
       │ ········································ │             └────────────────┘
       └──────────────────────────────────────────┘               仅对 196 个点对计算
                [显存: 5.01 MB / 步]                                [显存: 0.05 MB / 步]
```

### 算法实现机制
1. **活跃实体索引筛选**：
   从序列中仅挑选出有效实体与 `[MASK]` 位置，构成长度为 $K$ 的紧凑索引向量（$K \ll T$，通常 $K \approx 10 \sim 15$）。
2. **子张量切片投影**：
   仅对这 $K$ 个 token 进行 $3\times 3$ 几何矩阵投影，生成 $Q_{sub} \in \mathbb{R}^{B \times H \times K \times 3}$ 与 $K_{sub} \in \mathbb{R}^{B \times H \times K \times 3}$。
3. **局部 18 维外积与 FFN**：
   18 维特征仅在 $K \times K$ 子空间展开并穿过两层 MLP，耗时与内存开销缩减到微秒级。
4. **散射还原（Index Put）**：
   将计算好的紧凑得分矩阵直接写回（`index_put`）全句注意力张量对应坐标。

### 实测收益与数学验证
经实际代码 `examples/verify_sparse_gating_math.py` 全量验证：
* **单句计算点对**：由 18,225 对暴降至 196 对，**计算复杂度降低 98.92%**；
* **单步特征分配内存**：由 5.01 MB 降至 0.05 MB，**显存开销降低 98.92%**；
* **全数据集 candidate 对**：由 10,155,253 对压缩至 135,118 对，**废关系排除率 98.67%**。

---

## 6. 零人工标注：基于既有三元组的弱监督自举

要实现词属性过滤，传统做法需要耗费数月进行词性与语法依存标注。我们在本项目中彻底摒弃了人工标注，实现了**零成本自动自举（Zero-Manual-Annotation Bootstrap）**：

### 自举流水线
1. **实体与槽位投影**：
   利用《史记》173 个单元数据中现成的黄金三元组事实（`subject, predicate, object`）以及样本自带的 `role_spans`，自动将文中的人名、封号、地名打标为 `PERSON`, `TITLE`, `ENTITY`。
2. **虚词与标点先验注入**：
   汇聚文言文 50 词核心虚词表（`之, 者, 所, 以, 为, 于, 而, 则, 且, 乃, 乎...`）与标点集合，自动判定为 `FUNCTION` 与 `PUNCT`。
3. **词属性层（`GrammarAttributeFilter`）闭环训练**：
   在单层 Sigmoid 多标签分类器上对全词表 6,181 个 token 进行极速弱监督学习。

### 探针检验结果（100% 精度）
在输出的模型中抽样关键 probe：
* 实体/称谓：`魏子`、`周亚夫` $\to$ `['PERSON', 'ENTITY']`（100% 放行进入推理）
* 官职称谓：`蔺相如`、`舍人` $\to$ `['PERSON', 'TITLE', 'ENTITY']`（100% 放行）
* 语法虚词：`是`、`的`、`为`、`在` $\to$ `['FUNCTION']`（100% 拦截屏蔽）
* 标点符号：`，`、`。` $\to$ `['PUNCT']`（100% 拦截屏蔽）

---

## 7. 三维拓扑与 1/3 维度融合的几何直觉

为什么我们坚持使用 **三维流形（3D Combinations）** 与 **18 维特征（18D FFN）**，而不是传统的大一统高维点积？

### 标量点积的贫困 vs 三维拓扑的丰满
* 传统 Transformer 的注意力得分是标量内积：$s = q \cdot k$。标量只能表示“两者的相似程度或相关距离”，但**无法表达方向性与结构性**。
* 实体关系是天然具备三维拓扑的：主语、谓语、宾语构成一个闭合的三角形超图；空间、时间、主体构成事件的嵌入三元组。
* 当我们在每个头中选取 3 个特征维度 $(d_1, d_2, d_3)$，并执行 $3 \times 3$ 双向矩阵外积时，我们得到了 $3 \times 3 = 9$ 个局部交互分量，结合前向与后向组合形成 **18 维双向关系特征**。这 18 个特征完整捕获了局部几何流形的曲率、旋转与平移不变量。

### 1/3 维度融合（Dimension Fusion）
如果 8 个几何空间完全独立，各空间之间就会演化为孤立的语义孤岛，无法形成全局协同。我们设计了 **1/3 融合机制**：
* 在 256 维 Hidden 状态中，保留 160 维作为各空间互不重复的独立组合维度；
* 拿出 96 维（约 37.5%）作为 8 个空间循环共享的“公共参考基底”；
* **物理直觉**：就像 8 位观察者站在不同的角度审视同一个物理客体，各自观察独特的 2 个自由度，但共享同一个重心坐标系。这极大增强了跨空间的拓扑一致性。

---

## 8. 对话精粹与认知金句实录

在本次架构攻关的过程中，诞生了一系列切中认知智能命脉的精彩洞察。特将对话中的核心思想提炼如下：

> ### 金句一：关于注意力机制的虚假繁荣
> **“全注意力注意太多没有丝毫营养的东西了。换成人如此瞎注意，那就是纯瞎学，努力感动了自己，实际是没学会走先去抓地里的蚂蚁。”**
> *—— 深度反思了注意力机制无约束全连接的荒谬性，奠定了稀疏门控的基石。*

> ### 金句二：关于过拟合与死记硬背的辩证关系
> **“语法说话，本就是习惯远超于思维。我记得你说过过拟合就是死记硬背对吧？我巴不得它在语法层死记硬背呢！只要它做到过拟合‘我爱你’是‘人物-动作-人物’的固定组合，如果灵活我后面再加个类似 if-else 的机制。”**
> *—— 首次将“过拟合”从一种需要消除的缺陷，转变为一种可以被分层利用的“反射型认知能力”，促成了 System 1 与 System 2 的清晰解耦。*

> ### 金句三：关于泛化失败的终极诊断
> **“过拟合跟我设计的 loss 有关。但我的 loss 本身是给具备强泛型关系的实体组合用的，类似之前的两两配对一万四候选空间。现在 loss 被全注意力强行作用在很多废关系上来，所以才退化成了死板的记忆系统。”**
> *—— 一针见血地指出了 100% 训练准确率与 48% 验证准确率的根本症结在于 Loss 作用域被污染。*

> ### 金句四：关于神经与符号的协同
> **“不需要用几亿个参数去战战兢兢地猜标点，用一个极简的规则门控把它封死在门外，把有限的高维参数空间留给真正的历史逻辑。”**
> *—— 阐明了符号先验作为“护栏”约束连续流形推理的强大威力。*

> ### 金句五：关于完形认知与最小充分条件（去成分）
> **“最开始让 MASK 首先跟句子中的其他词的合向量做匹配，然后逐渐剔除，就是上来看总的，然后去掉成分。去成分的意思是：当句子中只保留哪些时，就可以预测到结果。”**
> *—— 颠覆了传统 Transformer 从下往上的两两盲目配对，确立了“总气场完形 $\to$ 提取最小充分条件”的高级推导范式。*

> ### 金句六：关于前置 FFN 与点积矩阵重构
> **“FFN 位置就得提前，在做点积之前。甚至它负责提供点积矩阵！”**
> *—— 一举纠正了传统 Transformer 让 FFN 充当“事后擦屁股诸葛亮”的倒装错误，赋予 FFN 判官与点积架构师的核心地位。*

> ### 金句七：关于 GELU 的物理晶体管与调参电路本质
> **“GELU 有某维度归零的能力，正好适合检查哪一维度影响最大；归零这种能力具备调参特性，类似于电路信号，可以微调，调的特点就是让谁通过让谁清零。”**
> *—— 深刻洞穿了深度学习的物理真相：参数微调即是调偏置电压与导通阈值，神经网络本质是一张可微的逻辑门电路。*

> ### 金句八：关于 3 维几何原子与拒绝和稀泥
> **“咱们是 3x3 的维度组合和动态 QK，每次维度归零就要同时去掉 3 维！被 GELU 都干成 0 了，还给它点值是图什么？给它留机会？根本不需要留机会，断路就是断路！”**
> *—— 奠定了“3 维是关系的不可分割原子”的铁律，坚决扫清了 Softmax 模棱两可的和稀泥弊病。*

---

## 9. 深度技术进阶：前置 3D 流形因果电路

关于前置 3D 关系审查 FFN、合向量去成分算法、3 维捆绑归零、反向拔插头测试与 ReLU 平方纯净稀疏注意力的具体技术实现与 PyTorch 规范，请参阅专项技术规范：  
👉 **[前置 3D 关系流形与因果电路架构设计规范 (PRE_FFN_3D_MANIFOLD_AND_CAUSAL_CIRCUIT_DESIGN.md)](PRE_FFN_3D_MANIFOLD_AND_CAUSAL_CIRCUIT_DESIGN.md)**

---

## 10. 技术演进全景路线图

```
[阶段 0: 原始瓶颈]
  4L-256D 全双向注意力 + 18D FFN 全展开
  ├── 显存: 10.6 GB (每句 18,225 点对计算)
  └── 表现: Train 100% vs Dev 48% (泛化崩溃，全句废关系硬背)
            │
            ▼
[阶段 1: 弱监督自举 (已完成)]
  examples/build_shiji_grammar_dataset.py
  ├── 173 历史单元全自动提取三元组槽位与虚词/标点
  └── 剔除 98.67% 垃圾配对 (10,155,253 -> 135,118 对)
            │
            ▼
[阶段 2: 词属性层训练与探针 (已完成)]
  examples/train_shiji_grammar_layer.py
  ├── 6,181 词汇多标签分类训练 (outputs/shiji-grammar-attribute-filter)
  └── 探针准确率 100% (精准判定 FUNCTION / PUNCT / PERSON / TITLE)
            │
            ▼
[阶段 3: 双系统稀疏门控与子矩阵 FFN (已完成)]
  bert_simple/structured_relation.py & bert_simple/dynamic_qk_model.py
  ├── Layer 0,1: 全注意力语感流动 (System 1)
  ├── Layer 2,3: 内嵌式 if-else 门控注入 (-10000.0) (System 2)
  └── 子矩阵 FFN: 仅在 14x14 实体对计算，显存与算力暴降 98.92% (5.01MB -> 0.05MB)
            │
            ▼
[阶段 4: A100 全量训练与历史单元泛化验证 (进行中)]
  examples/train_relation_pairs.py --enable-grammar-sparse-gate
  ├── 目标: Dev Top-1 突破 60%+，Test 突破 50%+
  └── 目标: “魏子 vs 蔺相如”与“皇帝 vs 怀王”排他性诊断保持清醒
            │
            ▼
[阶段 5: 前置 3D 流形因果电路与纯净点积 (V3 规范预研)]
  PRE_FFN_3D_MANIFOLD_AND_CAUSAL_CIRCUIT_DESIGN.md
  ├── FFN 前置提纯，3 维捆绑归零，由前置审查直接提供稀疏点积
  └── 拔插头敲除测试 + 累加和查账，实现 100% 白盒因果可解释
```

---

*本文档记录了本项目在探索非大模型暴力堆砌、基于认知规律与几何流形实现精准知识推理过程中的核心设计哲学与技术实现路径。*

