# 《史记》语法与词属性训练数据计划

## 当前结论

语法属性过滤器需要大量结构变化的句子。语法类别可以枚举，但模型必须在
不同主语、宾语、长度、分句、语气和词语组合中反复看到这些类别，才能把
结构规则泛化到没有见过的句子。

当前的主要目标是先训练独立的语法与词属性过滤器，再决定是否用它辅助
重训主 BERT。生成式问答暂时后置，等过滤器和主 BERT 的事实能力稳定后
再开始。

## 项目当前数据情况

当前项目实际只有一份原始训练文本：

`examples/shiji_baihua_zhangchen_gaozu_long_context.txt`

它有 100 行、约 8920 个空格分词 token，内容是《张耳陈余列传》和《高祖
本纪》等部分节选，不是完整《史记》。项目中的
`examples/build_shiji_long_context.py` 曾经引用 `examples/shiji_baihua.txt`，
但这个完整源文件当前不存在。

独立过滤器已经可以生成合成结构样本。目前生成的数量已经达到几千条，
但它们来自有限的结构模板和同义变体，归一化后有效结构仍然有限。合成
数据适合训练结构骨架和构造反例，不能替代真实史记语料。

## 原料目标

一万句是过滤器进入真实泛化测试的最低目标，建议先准备三万句以上；如果
原料允许，最好覆盖《史记》多个篇章，而不是只扩充高祖相关内容。

数据应保留三份，不要覆盖原始文件：

1. 原始版本：保留原文、篇章、段落和来源信息。
2. 句级版本：一句一行，按 `。！？；` 等句界切分，保留引号和标点归属。
3. 训练版本：在句级基础上进行空格分词、长度切块和样本构造。

当前的 `SimpleBertTokenizer` 依赖空格分词。训练版建议使用如下格式：

```text
高祖 在 长安 即位 。
他 派 使者 前往 沛县 。
```

如果拿到的是未分词的古文或白话原文，需要先做句界切分和分词，再接入
现有训练脚本。原始文本不能直接被当前 tokenizer 当作普通长句使用，否则
整段内容会被错误当成少数 token。

当前主模型使用的是白话分词语料。如果后续提供的是文言原文，建议保存
来源风格标记：白话和文言可以分别训练、分别评估，或在模型输入中加入
风格标记，不能默认把两种语法当成同一套分布。

## 需要覆盖的结构

当前的 12 类结构只作为第一版。真实数据应至少覆盖下面这些关系，并为每
种关系加入不同的正例、近似反例和多解句：

| 结构 | 典型槽位 | 需要覆盖的变化 |
| --- | --- | --- |
| 命名 | `叫/名为/称为 + 人名` | 人名、代词、官号、短语；`叫他去` 等兼语结构 |
| 地点 | `在/居住/前往 + 地点` | 地名、方位、地点短语、地点省略 |
| 时间 | `在/于 + 时间` | 年、月、日、时、期间、次序词 |
| 动作宾语 | `开始/正在/已经 + 动作` | 连续动作、结果补语、完成/进行状态 |
| 判断身份 | `是/为/任命为 + 身份` | 人物、官职、称号、组织、地点等补语 |
| 数量 | `有/共/约 + 数量` | 数字、数量词、人数、次数、范围 |
| 处置被动 | `把/被/使/令` | 受事、施事、结果和连续动作 |
| 条件原因 | `如果/若/因为/所以` | 前后分句、否定、原因与结果的顺序 |
| 并列转折 | `和/与/而/但/却` | 名词并列、动作并列、分句并列 |
| 助词语气 | `的/了/者/所/呢/吗` | 定中、完成、名词化、疑问、句末语气 |
| 否定范围 | `不/未/无/没有` | 否定动作、否定身份、否定数量和否定分句 |
| 句界 | `，。！？；` | 引号、分句插入、前后分句干扰 |
| 疑问 | `谁/哪/何时/什么/吗` | 人物、地点、时间和通用实体的多属性用法 |
| 省略与倒装 | 省略主语或宾语 | 古文常见省略、宾语前置、判断句倒装 |
| 兼语与连动 | `使/令/叫 + 人 + 动作` | 人物槽和动作槽同时出现 |

语法监督只需要标注“当前位置是什么槽位、允许哪些属性”，不需要人工
给整部《史记》的每个词都建立完整词典。

## 词属性学习方式

词属性分为三层：

1. 少量稳定种子：标点、`在/去/的/和/是/为/哪/谁` 等功能词和疑问词。
2. 内容锚点：少量地点、人物、官职、时间和数量词，用来固定属性空间
   的方向。
3. 未标注内容词：从 `UNKNOWN` 开始，由真实上下文、槽位监督和 MLM
   目标逐步学习，可以同时拥有多个属性。

同一个词必须出现在不同结构中。例如 `在` 需要同时覆盖：

```text
刘邦 在 沛县 起义 。
刘邦 在 夜间 出发 。
我 在 这里 。
我 在 呢 。
```

`在` 不能被固定成“后面一定是地点”。`哪`、`是`、`为`、`叫`、`呢`、
`吗` 也需要保留上下文决定属性，避免过滤层把合法用法删掉。

## 样本构造原则

真实句子进入过滤器训练时，建议同时构造四类样本：

1. 原句遮罩：保留真实上下文，遮住一个结构目标。
2. 局部替换：只替换目标附近的触发词，测试槽位是否随结构变化。
3. 近似反例：保持句子长度和主题相近，替换成不兼容属性的词。
4. 长上下文样本：在目标前后加入无关分句，测试过滤器是否只使用相关
   局部结构。

内容词可以在过滤器训练副本中归一化成通用占位符，避免过滤器记住某个
主语；主 BERT 训练仍保留真实词。过滤器必须保留原始候选词身份，不能
因为归一化而改变最终候选表。

## 数据划分

不能按随机句子简单切分。相邻句子经常共享同一段事实和同一套句式，随机
切分会让测试结果虚高。建议：

- 按篇章或段落划分训练、验证和测试；
- 训练/验证/测试约为 80%/10%/10%；
- 另外保存一套完全不参与调参的盲测篇章；
- 同一模板的改写不能同时出现在训练和盲测；
- 报告原始句数量、去重句数量、结构组合数量和重复率。

## 训练顺序

```text
大量史记原料
       ↓
句界切分、分词、篇章划分
       ↓
合成结构预训练 + 真实史记弱结构监督
       ↓
独立 8 维 g_t / 8 维 h_t + 独立 Q/K 过滤器
       ↓
真实盲测稳定后冻结过滤器
       ↓
用过滤器辅助重训主 BERT 和事实记忆
       ↓
重新评估三维动态 Q/K 与遗忘率
       ↓
最后再做生成式问答
```

过滤器阶段不更新主 BERT。主 BERT 重训阶段可以把冻结的过滤分数作为
低权重候选 bias 或辅助监督，但需要和“完全不接过滤器”的主模型做对照，
确认它确实提高事实 Top-1/Top-5，同时没有增加误过滤和遗忘。

## 进入主 BERT 重训的条件

在真实史记盲测上至少检查：

- 严格结构槽位准确率；
- 多解句的合法属性集合命中率；
- 人物、地点、时间、数量等属性准确率；
- 内容槽位中功能词和标点的压制率；
- 合法功能词、语气词和疑问词的误压制率；
- 加入无关分句后的局部槽位稳定性。

模板验证集达到 100% 不能作为放行条件。当前项目的模板训练结果很好，
但归一化后未见句式仍暴露出结构泛化不足，这正是需要更多史记原料的
原因。

## 需要的原料交付

拿到完整数据后，先不要覆盖当前文件。放入一个单独目录，例如：

```text
data/shiji/raw/
data/shiji/sentences/
data/shiji/segmented/
data/shiji/manifests/
```

每份数据附带来源、篇名、段落编号、句子编号和处理版本。这样可以在
训练后回答“模型学到的是语法，还是记住了某段文本”，也能保证盲测没有
泄漏。
